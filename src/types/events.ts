export enum Events {
  THEME_SET = 'theme.set',
  THEME_TOGGLE = 'theme.toggle',

  CALC_DIGIT = 'calc.digit',
  CALC_DECIMAL = 'calc.decimal',
  CALC_OPERATOR = 'calc.operator',
  CALC_EQUALS = 'calc.equals',
  CALC_CLEAR = 'calc.clear',
  CALC_DELETE = 'calc.delete',
}
